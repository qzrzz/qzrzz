// 这里列出的仓库排前面
const APPs = ["QLaunch", "Qjiao", "Qf", "AuBookPlayer"];
const PKGs = ["MinMPHash", "TableDB", "indexless"];

// 不显示的仓库
const HIDEs = ["ghostty"];

export default { APPs, PKGs, HIDEs };
